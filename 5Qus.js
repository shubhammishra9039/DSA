// 2. Flatten a nested array.


// Input:
// const arr = [1, [2, 3], [4, [5]]];
// Output:
// [1, 2, 3, 4, 5]


let arr= [1,[2,3],[4,[5]]]
let brr=[]

let tem=0
for(let i=0;i<=arr.length;i++)
{
    if(Array.isArray(arr[i]))
    {
        for(let j=0;arr[i].length<=0;j++)
        {
            if()
        }
    }
}

